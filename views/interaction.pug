block content
    head
        title Autorizza
    body
        h4 Stai autorizzando il trasferimento dei tuoi dati
        ul
            li DID ID
            li DID ID del tuo issuer
            li Nome
            li Cognome
            li Data di nascita
            li Telefono
            li Email
            li Codice fiscale
            li Sesso

        form#interactionForm(method="POST", action=`/interaction/${uid}/confirm`)
            button(type="submit") Conferma

        form#cancelForm(method="POST", action=`/interaction/${uid}/abort`)
            button(type="submit") Annulla
